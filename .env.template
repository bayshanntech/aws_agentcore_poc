# Claude API Configuration (for local development only)
ANTHROPIC_API_KEY=your_claude_api_key_here_for_local_testing

# AWS Configuration (optional - can use AWS CLI profile instead)
AWS_REGION=us-east-1
AWS_PROFILE=default

# AgentCore Configuration (for AWS deployment - uses outbound identity)
AGENTCORE_OUTBOUND_IDENTITY_ARN=arn:aws:bedrock-agentcore:us-east-1:713881785922:outbound-identity/Anthropic-Key-Diego